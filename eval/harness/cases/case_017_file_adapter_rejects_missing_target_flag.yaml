id: case_017_file_adapter_rejects_missing_target_flag
description: File adapter rejects startup with no target mode.
timeout_secs: 10
spec: |
  syva-file must require exactly one of --core-socket or --cp-endpoint. With
  neither flag set, it exits non-zero quickly with a clear required-target
  message.
