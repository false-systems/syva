id: case_039_api_missing_core_socket_waits_without_crashing
description: API adapter tolerates a missing core socket at startup.
timeout_secs: 10
spec: |
  syva-api started with --core-socket pointing at a non-existent path must not
  crash immediately; it may wait for the core socket before binding HTTP.
