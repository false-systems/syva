/target
**/target

# Harness run artifacts — regenerated every run.
eval/results/*.json

# Local AI-assistant session state.
.claude/
.codex

# Local Sykli run history; sykli.rs and sykli.lock are the source contract.
.sykli/
